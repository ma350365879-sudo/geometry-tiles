# 贡献指南

感谢您有兴趣为「几何拼接片（Geometric Tiles）」做贡献。

--------------------------------------------------------------------------------

## ⚠️ 开始之前：请先阅读 CLA

本项目采用 **[CLA（贡献者许可协议）](CLA.md)**。

**在提交第一个 Pull Request 之前，您必须先同意 CLA。** 通过提交 PR
即视为您已同意。

**为什么需要 CLA？** 本项目保留就卡扣连接结构向规模化生产厂商提供
商业授权的可能（详见 [NOTICE](NOTICE) 第二部分）。为在将来能够合法地
进行此类授权，权利人需要就全部贡献获得不受许可证种类限制的著作权
许可。这不是针对您的限制，而是保证项目可持续的必要机制。

**若您不同意 CLA，请不要提交 PR。** 您仍可自由使用本项目（依
[Apache License 2.0](LICENSE)），并通过 Issue 反馈问题与建议。

--------------------------------------------------------------------------------

## 可以贡献什么

| 类型 | 说明 | 是否需要 CLA |
|---|---|---|
| 🐛 Bug 报告 | 通过 Issue 描述问题 | 否 |
| 💡 功能建议 | 通过 Issue 提出想法 | 否 |
| 📖 文档改进 | 修正错别字、补充说明 | **是** |
| 🔧 代码贡献 | 修 bug、加功能、重构 | **是** |
| 🧩 形状预设 | 新增密铺形状配方 | **是** |
| 🖼️ 示例图 | 补充测试用例图册 | **是** |

简单的反馈和讨论**不需要** CLA，欢迎直接在 Issue 里聊。

--------------------------------------------------------------------------------

## 开发环境

```bash
# 1. 克隆
git clone <仓库地址>
cd geometry-tiles

# 2. 安装依赖（需要 Python 3.9+）
pip install -r requirements.txt     # build123d==0.8.0

# 3. 启动
python3 tile_generator_v3.py
# 浏览器访问 http://127.0.0.1:8768
```

**注意**：3D 构建在独立子进程执行，**修改后端代码后需重启服务**；
前端（HTML）改动刷新页面即可。

--------------------------------------------------------------------------------

## 测试

```bash
python3 test_wrap_segments.py
```

31 个回归用例，输出 `valid/solids/connected/clips/包边段/体积` 等指标。

**提交 PR 前请确保：**

1. 测试全部通过（或说明失败原因与是否为既有问题）
2. 新增功能请补充对应测试用例
3. 不要在测试中引入需要长时间（>60s）构建的形状

更完整的验收矩阵见 [docs/复现需求文档_V3.md](docs/复现需求文档_V3.md) 第 4、7 节。

--------------------------------------------------------------------------------

## 提交 PR 的流程

```
1. Fork 本仓库
2. 创建分支：git checkout -b fix/你的修改描述
3. 修改并本地验证（跑测试 + 实际启动页面确认）
4. 提交：git commit -m "fix: 简明描述"
5. 推送并创建 Pull Request
6. 在 PR 中回复 CLA 同意声明（首次贡献时）
```

### Commit message 建议格式

```
feat: 新增功能
fix: 修复问题
docs: 文档变更
refactor: 重构（不改变行为）
test: 测试相关
chore: 构建/工具链
```

--------------------------------------------------------------------------------

## 代码约定

**Python（`tile_generator_v3.py`）**

- 保持单文件结构 —— 这是项目的刻意设计，降低复现与部署门槛
- 几何相关常量集中在文件头部，便于调整
- 中文注释，与现有风格一致
- 涉及几何容差（`*_TOL`、`*_GAP`、`*_OVERLAP`）的修改**必须说明理由** ——
  这些值经过大量实测调优，随意改动会导致布尔运算失败或产生薄片

**前端（`tile_generator_v3.html`）**

- 单文件应用（原生 JS + Three.js），**不引入构建工具**
- Three.js 已本地化于 `vendor/three/`，请勿改为 CDN 引用（离线运行是需求）

**几何预设**

新增形状预设时，请同时提供：

- 参数用例文本（高级区可粘贴的格式）
- 2D 轮廓示例图（放入 `docs/测试用例图册/`）
- 验证该预设的测试用例

--------------------------------------------------------------------------------

## 已知限制（不是 bug）

以下为**已知且已记录**的限制，相关 Issue 可能不会被优先处理：

- 部分退化形状（边过短、凹角 + 弧组合）按段跳过并给出提示
- 极少数用例的卡扣可能无法连接（测试套件中 2 个用例即属此类）
- 底层布尔运算在个别几何上可能极慢（已内置 60s 看门狗）
- 仅适配桌面浏览器，不支持移动端
- 并发请求为串行处理（单 worker 子进程架构）

详见 [README.md](README.md) 与 [NOTICE](NOTICE)。

--------------------------------------------------------------------------------

## 关于专利

本项目的卡扣连接结构涉及专利申请（已受理）。**请您注意：**

- 您可以在 CLA 第 2 条许可范围内自由贡献代码
- 若您的贡献涉及可能与上述专利冲突的技术方案，请在 PR 中说明，
  我们将共同评估
- **请勿在贡献中引入任何您无权许可的第三方专利技术**

详见 [NOTICE](NOTICE) 第二部分。

--------------------------------------------------------------------------------

## 行为准则

- 保持友善与专业，就技术论技术
- 尊重不同经验水平的贡献者
- 不欢迎任何形式的骚扰、歧视或人身攻击

--------------------------------------------------------------------------------

## 联系方式

- 问题反馈：GitHub Issues
- 商业授权咨询：@顺顺爸爸啊
