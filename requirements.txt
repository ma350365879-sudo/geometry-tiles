# 几何内核。build123d 0.8.0 对 cadquery-ocp 只声明 >=7.7.0（无上界），
# 若不钉版本，pip 会解析到较新的 OCP，其 NCollection API 已变更，
# 导致 `from OCP.NCollection import NCollection_Utf8String` 导入失败。
# 7.7.2 为本项目实际验证通过的版本，请勿随意升级。
build123d==0.8.0
cadquery-ocp==7.7.2
